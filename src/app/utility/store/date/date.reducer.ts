import {Action} from "@ngrx/store";

export const initialValue = new Date().toLocaleTimeString()
const DATE = 'DATE'

// export const dateReducer = (state = initialValue, action: Action) => {
//   switch (action.type){
//     case DATE:
//       return state
//   }
// }
